---
import type { Book } from '../types';
import BookCard from './BookCard.astro';

interface Props {
	books: Book[];
}

const { books } = Astro.props;
---

<div class="grid gap-x-16 gap-y-20 md:grid-cols-2 lg:gap-x-24 lg:gap-y-28">
	{books.map((book) => <BookCard book={book} />)}
</div>
