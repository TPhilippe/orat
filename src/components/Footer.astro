---
import { navigation, site } from '../data/site';
import { getHouse } from '../services/house';

const house = await getHouse();
const year = new Date().getFullYear();
---

<footer class="mt-4 border-t border-line">
	<div class="mx-auto grid max-w-[1400px] gap-10 px-6 py-12 md:grid-cols-3 md:px-12 lg:gap-16 lg:px-16 lg:py-14">
		<div>
			<img
				src="/logos/orat.png"
				alt={site.name}
				width="220"
				height="58"
				class="h-8 w-auto"
			/>
			<p class="mt-4 max-w-xs font-body text-xl leading-relaxed text-ink">{house.tagline}</p>
		</div>

		<div>
			<p class="label mb-4">Parcourir</p>
			<ul class="space-y-2">
				{
					navigation.map((item) => (
						<li>
							<a href={item.href} class="font-body text-xl text-ink transition-colors hover:text-accent">
								{item.label}
							</a>
						</li>
					))
				}
			</ul>
		</div>

		<div>
			<p class="label mb-4">Correspondance</p>
			<p class="font-body text-xl leading-relaxed text-ink">
				{house.address.map((line) => (
					<>
						{line}
						<br />
					</>
				))}
			</p>
			<a href={`mailto:${house.email}`} class="mt-3 inline-block font-body text-xl font-medium text-ink hover:text-accent">
				{house.email}
			</a>
		</div>
	</div>

	<div
		class="mx-auto flex max-w-[1400px] flex-col gap-2 border-t border-line px-6 py-5 font-body text-base text-ink-muted md:flex-row md:items-center md:justify-between md:px-12 lg:px-16"
	>
		<p>© {year} {house.legalName}</p>
		<p>{house.city}</p>
	</div>
</footer>
